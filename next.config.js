/** @type {import('next').NextConfig} */
const nextConfig = {
  // configure image domains
  images: {
    domains: [
      "images.unsplash.com",
      "via.placeholder.com",
      "unsplash.it",
      "source.unsplash.com",
      "picsum.photos",
    ],
  },
};

module.exports = nextConfig
